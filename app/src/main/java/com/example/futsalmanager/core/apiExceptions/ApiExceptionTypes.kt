package com.example.futsalmanager.core.apiExceptions

enum class ApiExceptionTypes {
    INVALID_CREDENTIALS,
    EMAIL_NOT_VERIFIED,
    CONFLICT,
    INVALID_OTP,
    SESSION_INVALID,
    INTERNAL_ERROR,
    VALIDATION_ERROR,
    INVALID_TOKEN
}