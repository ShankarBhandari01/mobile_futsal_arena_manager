package com.example.core_data.data.remote.api.apiExceptions

enum class ApiExceptionTypes {
    INVALID_CREDENTIALS,
    EMAIL_NOT_VERIFIED,
    CONFLICT,
    INVALID_OTP,
    SESSION_INVALID,
    INTERNAL_ERROR,
    VALIDATION_ERROR,
    INVALID_TOKEN
}